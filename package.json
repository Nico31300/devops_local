{
	"name": "web-app-name",
	"version": "2.0.0",
	"description": "Project description",
	"devDependencies": {
	},
	"scripts": {
		"karma": "karma start",
		"clean": "rm -rf node_modules",
		"reinstall": "npm run clean && npm install",
		"opa": "npm run karma"
	}
}
